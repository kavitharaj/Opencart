
package testcases;

import static org.testng.Assert.assertTrue;

import org.openqa.selenium.WebDriver;
import org.testng.Assert;
import org.testng.annotations.Test;

import Baseclass.baseClass;
import pageObjects.BasePage;
import pageObjects.Homepage;
import pageObjects.Loginpage;
import pageObjects.Myaccountpage;
import utilities.dataProvides;

public class tc003_datadrivenlogintest extends baseClass {

	@Test(dataProvider = "Login data", dataProviderClass = dataProvides.class)
	public void verify_loginddt(String email, String pw, String result) {

		Homepage hp = new Homepage(driver);
		hp.clickmyaccount();
		hp.clicklogin();

		/*
		 * Loginpage lp = new Loginpage(driver);
		 * 
		 * lp.setemail(email); lp.setpassword(pw); lp.clicksubmit();
		 * 
		 * Myaccountpage mcc = new Myaccountpage(driver); boolean targetpage =
		 * mcc.ismyAccountexists();
		 * 
		 * if (targetpage == true) { mcc.clicklogout(); Assert.assertTrue(true); } else
		 * Assert.assertTrue(false);
		 */
	
	
		/*
		 * if (result.equalsIgnoreCase("invalid")) { if (targetpage == true) {
		 * mcc.clicklogout(); Assert.assertTrue(false); } else Assert.assertTrue(true);
		 * 
		 * }
		 */


}}
